<%= form_with(url: edit_filename_post_course_tut_path(params[:blob_id]), local: true) do |f| %>
  <%= f.label :file_name %>
  <%= f.text_field :file_name, value: ActiveStorage::Blob.find_by(id: params[:blob_id]).filename %>
  <%= f.submit %>
<% end %>